import PropTypes from 'prop-types';

const TABLE_ROW = {
    id: PropTypes.number,
    ip: PropTypes.string,
    mac: PropTypes.string,
    domain: PropTypes.string,
    disabled: PropTypes.bool
};

export default {
    TABLE_ROW
};
